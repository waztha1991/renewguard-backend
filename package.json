{
  "name": "renewguard-cloudflare-deploy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker + Container deploy wrapper for the RenewGuard Ktor backend",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.30"
  },
  "devDependencies": {
    "wrangler": "^4.22.0"
  }
}
